export default () => {
    // Início do código
    return (
        // Informação de retorno do erro
        <div className="flex w-full h-screen items-center justify-center">
            <h1 className="font-bold text-xl text-blue">Ainda em produção...</h1>
        </div>
    )
}