export default () => {
    // Início do código
    return (
        // Mensagem de retorno do carregamento da página
        <div className="flex w-full h-screen items-center justify-center">
            <h1 className="font-bold text-xl text-blue">Carregando...</h1>
        </div>
    )
}